spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    show-sql: true

security:
  jwt:
    requestHeader: authorization
    secretKey: testSecretKey-testSecretKey-testSecretKey
    validity: 36
  default-users:
    admin:
      username: test
      password: test
