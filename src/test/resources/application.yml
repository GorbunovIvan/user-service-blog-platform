spring:
  config:
    import: "optional:configserver:"

security:
  jwt:
    requestHeader: authorization
    secretKey: testSecretKey-testSecretKey-testSecretKey
    validity: 36 # in seconds
  default-users:
    admin:
      username: test
      password: test
